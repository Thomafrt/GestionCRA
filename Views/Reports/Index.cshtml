@model IEnumerable<CRA.Models.Domain.Report>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BeginWeek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndWeek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entries)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BeginWeek)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndWeek)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entries)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
